// Modern route mapping for panda plugin
export default function () {
  // This would add client-side routes if needed
  // For now, we're using server-side routing to /panda
  // But this demonstrates how to add client-side routes in Discourse plugins
  
  // Example of how you might add a client-side route:
  // this.route('panda-stats', { path: '/panda/stats' });
}
